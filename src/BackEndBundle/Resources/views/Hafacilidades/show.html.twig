{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hafacilidades</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Icono</th>
                <td>{{ entity.icono }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ entity.imagen }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hafacilidades') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hafacilidades_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
