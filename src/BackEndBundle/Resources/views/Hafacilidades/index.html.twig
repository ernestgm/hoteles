{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hafacilidades list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Icono</th>
                <th>Imagen</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hafacilidades_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.icono }}</td>
                <td>{{ entity.imagen }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('hafacilidades_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('hafacilidades_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hafacilidades_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
