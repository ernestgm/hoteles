<div class="ibox-content">
    <span class="text-muted small pull-right">
      <div class="ibox-tools">
          <a ng-show="ho.hotel.add_gast_show != 0" ng-click="ho.addGastronomia(ho.hotel.hotel_selected)" class="btn btn-primary btn-xs">
              <i class="fa fa-plus"></i>
          </a>
      </div>
    </span>
    <h2>{{ 'Gastronomía' | trans }}</h2>
<div class="table-responsive">
    <table class="table table-striped">
        <thead>
        <tr>
            <th>{{ 'Nombre' | trans }}</th>
            <th style="text-align:right">{{ 'Acciones' | trans }}</th>
        </tr>
        </thead>
        {% verbatim %}
        <tbody>
        <tr ng-repeat="gastronomia in ho.hotel.hotel_gastronomia">
            <td>{{ gastronomia.nombre }}</td>
            <td class="ibox-tools">
                <a href=""><i class="fa fa-cog"></i></a>
                <a ng-click="ho.removeGastronomia(gastronomia.id)" href=""><i class="fa fa-trash-o"></i></a>
            </td>
        </tr>
        </tbody>
        {% endverbatim %}
    </table>
</div>
</div>