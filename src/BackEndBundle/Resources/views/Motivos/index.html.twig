{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Motivos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Wsid</th>
                <th>Descripcion</th>
                <th>Imagen</th>
                <th>Orden</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('motivos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.wsId }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.imagen }}</td>
                <td>{{ entity.orden }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('motivos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('motivos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('motivos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
